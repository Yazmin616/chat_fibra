# ── Servidor y Base de Datos ──────────────────────────────────────────────────
PORT=3009
DB_HOST=localhost
DB_PORT=5432
DB_NAME=isp_chatbot
DB_USER=postgres
DB_PASSWORD=tu_password

# ── JWT Autenticación ─────────────────────────────────────────────────────────
JWT_SECRET=fibratec_secret_2026
JWT_EXPIRY=10h

# ── Servidor SMTP (Recuperación de Contraseña por Correo) ──────────────────────
# Ejemplo con Gmail:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=notificaciones@fibratec.mx
# SMTP_PASS=tu_app_password
# SMTP_FROM=notificaciones@fibratec.mx
#
# Ejemplo con Servidor Propio / cPanel / Office 365:
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
